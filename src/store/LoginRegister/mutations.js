export const REGISTER_USER = (state, payload) => {
  state.register_user = payload;
};

export const LOGIN_USER = (state, payload) => {
  state.login_user = payload;
};

export const USER_DETAILS = (state, payload) => {
  state.user_details = payload;
};
